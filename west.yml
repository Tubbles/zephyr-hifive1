# Single source of truth for which Zephyr (and modules) this project builds
# against. Bump `revision`, rebuild the image, done. This repo is itself the
# manifest repo (Zephyr "T2 / star topology" application).
manifest:
  remotes:
    - name: zephyrproject-rtos
      url-base: https://github.com/zephyrproject-rtos
  projects:
    - name: zephyr
      remote: zephyrproject-rtos
      revision: v4.4.1
      # Import Zephyr's manifest -- this is also how west learns the build/flash
      # extension commands -- but pull only the modules this board needs.
      # FE310 / HiFive1 support is entirely in-tree and picolibc comes from the
      # SDK toolchain (CONFIG_PICOLIBC_USE_TOOLCHAIN), so nothing external is
      # required: the allowlist is empty. Add a module name here if you enable a
      # feature that needs one (e.g. a sensor HAL, a filesystem, mbedTLS).
      import:
        name-allowlist: []
  self:
    # The workspace topdir is the repo root; `make update` copies this file
    # into a generated, gitignored `.manifest/` git repo and runs
    # `west init -l .manifest`, so the checkouts (zephyr/, modules/, ...) land
    # alongside it under the repo. This path is relative to that topdir.
    path: .manifest
